using UnityEngine;

namespace FasterGames.PackageTemplate.Editor
{
    public class EditorExample : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}